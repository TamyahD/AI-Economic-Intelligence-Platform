server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/economic_platform
    username: economic_user
    password: TdPassword111
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO